package app.GUI.Bindings;

/**
 * Enumeration to select the necessary component to compute by a binding
 * Ax -> Horizontal component
 * Ay -> Vertical component
 */
public enum VEC {
    Ax,
    Ay
}
